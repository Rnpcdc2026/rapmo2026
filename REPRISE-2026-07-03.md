# Pense-bête de reprise — RAPMO 2026

_Session du 3 juillet 2026. Lancement décalé : test lundi, invitations mardi._

## ✅ Fait aujourd'hui

### Lot 1 — Accès & données
- Code d'accès public **retiré** (middleware.ts) → plateforme publique, seul /admin protégé
- /api/invitees : contrôle cookie retiré + colonne `filiere` renvoyée
- Formulaire : filière affichée/cherchable dans l'autocomplete
- SQL exécutés : réimport **216 invitations** (215 du nouveau xlsx + Eric test) avec filière ; **jauges** (St Priest 50 / autres visites site 55 · « Sous le même toit » 50 / autres confs 55 · St Jean 50 / Festival 55 / Musée des Confluences sans jauge)
- Décision assumée : emails invités exposés via l'API publique (RGPD, choix Eric d'ouvrir l'accès)

### Lot 2 — Contenu
- Page 1 : nouveau titre + sous-titre + texte (docx définitif)
- « début septembre » → « en septembre » (formulaire + mail)
- Récap allégé : hébergement + transport retirés (step 4 + mail), régime seul
- SQL exécuté : titres/descriptions **définitifs** visites & ateliers (« Sous le même toit », « Réhabiliter autrement… », « Réflexion autour du projet stratégique », « matériaux biosourcés »)

### Lot 3 — Programme + photos
- Page /programme **reconstruite** (déroulé jeudi/vendredi + descriptions, docx)
- Page 1 : illustration Confluence ajoutée (public/home-confluence.jpg)
- 4 photos intégrées + compressées : Saint-Priest, Gratte-Ciel Nord, Albizzia (visite-pierre-taille-mariniers), home-confluence
- .gitignore : temporaires images + .claude/

## ⏳ En cours / à décider
- **Refonte mise en page** (maquette validée ? à confirmer) : Page 1 (hero 2 colonnes + barre Dates/Lieu/programme), page programme (pastilles horaires rouges + cartes photo), page accès (cartes transport + icônes). Direction montrée en maquette CDC — **attente de ton feu vert** pour implémenter page par page (commencer par /programme).

## ⏳ Reste à faire
- **Crédits photos** : patrimoine = à CDC (pas de crédit). Villeurbanne/Gratte-Ciel = crédit à venir (Nadia demande à l'équipe Lyon) → à ajouter en légende quand reçu.
- **Bloc F** (Step 3) : régime dropdown obligatoire + colonne `allergies` + hôtel facultatif. ⚠️ Touche la fonction Postgres `register_participant` (non versionnée) — récupérer sa def via `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname='register_participant';`
- Supprimer les fichiers temporaires `public/visits/_new-*.jpg` via le Finder (ignorés par git, pas urgent)
- Confirmer avec Eric : mail test en boîte de réception (pas spam)
- 1 invité supplémentaire possible (confirmation lundi) → nouvelle version du xlsx à réimporter le cas échéant

## Notes techniques
- `next.config.js` : `ignoreBuildErrors` + `ignoreDuringBuilds` = true → build Vercel ne casse pas sur TS/ESLint
- SQL versionnés dans supabase/ : import-invites, jauges, contenu-visites-ateliers (2026-07)
- Le mount ne permet pas de supprimer des fichiers (unlink) → renommages OK, suppressions à faire côté Finder
